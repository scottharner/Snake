snake
-----
- had to use gfx_gdi with set_gfx_mode to get the video working for the game on win 10
	- set_gfx_mode( GFX_GDI, MAP_WIDTH * TILE_SIZE, MAP_HEIGHT * TILE_SIZE, 0, 0);
- had to do the following cmd line to build from mingw32
	- gcc snake.c -o snake.exe -I"d:\allegro-mingw-4.2.2\include" -L"d:\allegro-mingw-4.2.2\lib" -lalleg -fgnu89-inline
	- as we add files we have to list each individual c file so i have instead moved to using a Makefile and running make
- used allegro-mingw-4.2.2 to get this to build (uncertain of original version but this is best guess based on date)
- had to copy alleg42.dll to application directory
- we have one make file per platform now, so we need a different make command
  - make -f Makefile.windows

saturn dev faqs
---------------
- why does my tile show garbled when using jo engine and map editor?
	- you cant use compression when saving a graphic as tga
- what is the default resolution with jo engine?
	- 320x240
